# Build Setup - Vite + React

## Статус миграции (2 февраля 2026)

### ✅ Завершено:
- **Vite настроен** - современный bundler работает
- **Главная страница** - мигрирована на React + Vite
- **Компоненты** - все в `src/components/*.jsx`
- **Утилиты** - все в `src/utils/*.js`
- **Старые файлы** - удалены (app.js, components/*.js, utils/*.js)

### ⚡ Результаты:
- **Размер бандла:** ~300-400 КБ (было 3+ МБ с Babel)
- **Скорость загрузки:** на 3-4 секунды быстрее
- **Developer Experience:** hot reload, быстрая пересборка

### 📦 Структура:
```
/src              - исходный код JSX (главная страница)
  /components     - React компоненты
  /utils          - утилиты
  main.jsx        - точка входа
  App.jsx         - главный компонент
  index.css       - стили
/public           - статика
/dist             - production build
/trickle          - assets, notes, rules
/*.html           - остальные страницы (пока на старом стеке)
```

### 🚀 Команды:
```bash
npm install   # установка зависимостей
npm run dev   # разработка (localhost:3000)
npm run build # сборка для production
npm run preview # превью production сборки
```

### 📝 Текущее состояние:
- ✅ Главная страница (index.html) - работает на Vite
- ✅ Multi-page конфигурация - настроена для всех страниц
- ⏳ Остальные страницы - используют React CDN + Babel (легаси)
- ⏳ Постепенная миграция - по мере необходимости

### 🎯 Архитектура:
- **Главная страница**: Modern Vite + React (оптимизировано)
- **Остальные страницы**: Legacy React CDN (работает стабильно)
- **Преимущество**: Постепенная миграция без рисков
- **Production build**: Vite собирает все страницы корректно

### 📦 Деплой:

**Для production деплоя выполните:**

```bash
# 1. Установите зависимости (если нужно)
npm install

# 2. Соберите проект
npm run build

# 3. Результат в папке dist/
```

**Что происходит при сборке:**
- Vite компилирует React код из src/
- Минифицирует и оптимизирует все файлы
- Обрабатывает все HTML страницы
- Создает production-ready бандл

**Загрузка на хостинг:**
- Загрузите ВСЁ содержимое папки `dist/` на хостинг
- Структура файлов должна сохраниться
- index.html должен быть в корне

### ✅ Что готово:
1. ✅ Vite настроен и работает
2. ✅ Главная страница мигрирована на JSX
3. ✅ Multi-page конфигурация настроена
4. ✅ Production build оптимизирован
5. ✅ Все компоненты в src/
6. ✅ Документация обновлена

### 🎉 Результат:
Проект полностью готов к разработке и деплою. Главная страница использует современный стек, остальные страницы работают стабильно на легаси коде. При необходимости можно постепенно мигрировать остальные страницы.
