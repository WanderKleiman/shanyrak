# 🚀 Shanyrak Charity Platform - Build Guide

## Быстрый старт

### 1. Установка зависимостей
```bash
npm install
```

### 2. Режим разработки
```bash
npm run dev
```
Откройте http://localhost:3000

### 3. Сборка для production
```bash
npm run build
```
Файлы появятся в папке `dist/`

### 4. Превью production сборки
```bash
npm run preview
```

## Структура проекта

- `src/` - исходный код главной страницы (JSX компоненты)
- `public/` - статические файлы
- `dist/` - собранный production код
- `trickle/` - ассеты, заметки, правила
- `*.html` - остальные страницы (легаси React)

## Архитектура

**Гибридная структура:**
- Главная страница (index.html): современный Vite + React
- Остальные страницы: стабильный React CDN + Babel
- Все страницы собираются через Vite для production

## Деплой

### ⚠️ КРИТИЧЕСКИ ВАЖНО:

**Вы НЕ МОЖЕТЕ просто открыть index.html в браузере!**

Проект использует Vite + React и требует сборки для production.

### Шаги для production деплоя:

1. **Установите зависимости**:
   ```bash
   npm install
   ```

2. **Соберите проект для production**:
   ```bash
   npm run build
   ```

3. **Результат сборки**:
   - Все файлы будут в папке `dist/`
   - Vite скомпилирует JSX → JavaScript
   - React код будет минифицирован
   - Все модули будут объединены

4. **Загрузите на хостинг**:
   - Загрузите **ВСЁ СОДЕРЖИМОЕ** папки `dist/` на хостинг
   - НЕ загружайте корневую папку!
   - Загружайте только то, что внутри `dist/`

5. **Проверка**:
   - Откройте сайт через хостинг (не локально!)
   - Проверьте консоль браузера на ошибки
   - Готово! ✨

### ❌ Что НЕ НУЖНО делать:
- НЕ открывайте index.html напрямую в браузере
- НЕ загружайте исходники (src/, node_modules/)
- НЕ используйте проект без сборки

### ✅ Правильная последовательность:
```
npm install → npm run build → загрузить dist/ → открыть через хостинг
```

## Преимущества

- ⚡ Быстрая загрузка (~300 КБ вместо 3+ МБ)
- 🎯 Оптимизация кода
- 📦 Минификация и gzip
- 🔥 Hot Module Replacement
- ✨ Современный JSX синтаксис

## Деплой

### ⚠️ КРИТИЧЕСКИ ВАЖНО:

**Вы НЕ МОЖЕТЕ просто открыть index.html в браузере!**

Проект использует Vite + React и требует сборки для production.

### Шаги для production деплоя:

1. **Установите зависимости**:
   ```bash
   npm install
   ```

2. **Соберите проект для production**:
   ```bash
   npm run build
   ```

3. **Результат сборки**:
   - Все файлы будут в папке `dist/`
   - Vite скомпилирует JSX → JavaScript
   - React код будет минифицирован
   - Все модули будут объединены

4. **Загрузите на хостинг**:
   - Загрузите **ВСЁ СОДЕРЖИМОЕ** папки `dist/` на хостинг
   - НЕ загружайте корневую папку!
   - Загружайте только то, что внутри `dist/`

5. **Проверка**:
   - Откройте сайт через хостинг (не локально!)
   - Проверьте консоль браузера на ошибки
   - Готово! ✨

### ❌ Что НЕ НУЖНО делать:
- НЕ открывайте index.html напрямую в браузере
- НЕ загружайте исходники (src/, node_modules/)
- НЕ используйте проект без сборки

### ✅ Правильная последовательность:
```
npm install → npm run build → загрузить dist/ → открыть через хостинг
```

## Требования

- Node.js 18+ 
- npm или yarn

## Поддержка

Вопросы? info@shanyraq.ru
